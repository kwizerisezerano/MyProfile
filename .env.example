# GitHub Personal Access Token (required for real contribution data)
# Get yours at: https://github.com/settings/tokens
# Required scope: read:user
# IMPORTANT: This token is exposed client-side (NEXT_PUBLIC_ prefix required for static export)
# Use a token with minimal permissions (read-only, read:user scope only)
NEXT_PUBLIC_GITHUB_TOKEN=
